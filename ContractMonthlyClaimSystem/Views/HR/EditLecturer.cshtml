@model ContractMonthlyClaimSystem.Models.ApplicationUser
@{
    ViewData["Title"] = "Edit Lecturer";
}

<div class="container col-md-6">
    <div class="card shadow">
        <div class="card-header bg-warning text-dark">
            <h4 class="mb-0">Edit Lecturer Details</h4>
        </div>
        <div class="card-body">
            @if (ViewBag.Message != null)
            {
                <div class="alert alert-success">@ViewBag.Message</div>
            }

            <form asp-action="EditLecturer" method="post">
                <input type="hidden" name="id" value="@Model.Id" />

                <div class="mb-3">
                    <label class="form-label">First Name</label>
                    <input name="firstName" value="@Model.FirstName" class="form-control" required />
                </div>
                <div class="mb-3">
                    <label class="form-label">Last Name</label>
                    <input name="lastName" value="@Model.LastName" class="form-control" required />
                </div>
                <div class="mb-3">
                    <label class="form-label">Email</label>
                    <input name="email" value="@Model.Email" class="form-control" required />
                </div>
                <div class="mb-3">
                    <label class="form-label">Hourly Rate (ZAR)</label>
                    <input name="hourlyRate" value="@Model.HourlyRate.ToString("F2").Replace(",", ".")" type="number" step="0.01" class="form-control" required />
                </div>

                <button type="submit" class="btn btn-success w-100">Save Changes</button>
                <a asp-action="ManageLecturers" class="btn btn-link d-block text-center mt-2">Cancel</a>
            </form>
        </div>
    </div>
</div>